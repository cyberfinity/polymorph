---
import type { HTMLAttributes } from "astro/types";

interface Props extends HTMLAttributes<"header"> {}

const { ...headerProps } = Astro.props;
---

<header {...headerProps}>
  <slot />
</header>
